# fetchApi
In order to test this API, you should follow the next steps:

1. Clone the repository
2. Open in your prefer IDE and then in a console run
   `npm install` 
3. In order to test locally, run the next command
   `serverless invoke local -f fetchApi`

Made with :heart: by @jaruco
